# Migration Guide

Halo UI starts at `0.1.0`, so there are no previous public versions to migrate from yet.

Future breaking changes must document:

- Changed component names or import paths.
- Changed props, events, slots, or service options.
- Changed theme token names.
- Platform compatibility changes.
- Required manual steps for `uni_modules` and npm users.

