# Components

Wbbb UI contains 47 components across Basic, Layout, Form, Feedback, and Display categories. Each component page includes API data, state coverage, on-demand imports, and a live mobile preview.

```ts
import { WbbbButton } from 'wbbb-ui/components/button'
import 'wbbb-ui/styles/button'
```
